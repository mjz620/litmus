export default function Home() {
  return (
    <main className="landing-page">
      <section className="hero" aria-labelledby="page-title">
        <p className="eyebrow">AI-native chemistry rehearsal</p>
        <h1 id="page-title">LabBench AI</h1>
        <p className="summary">
          Practice virtual chemistry experiments, learn from meaningful actions,
          and build confidence before stepping into the lab.
        </p>
        <p className="status">The first experiment is being prepared.</p>
      </section>
    </main>
  );
}
