export const LEGACY_TITRATION_RUNTIME_ADAPTER = Object.freeze({
  id: "runtime-adapter.titration.v1",
  version: "1.0.0",
  engineId: "engine.titration.v1",
  experimentDefinitionId: "acid_base_titration"
} as const);

export const LEGACY_TITRATION_CHEMISTRY_MODEL = Object.freeze({
  id: "chemistry-model.legacy_titration.v1",
  version: "1.0.0"
} as const);
